import styled from 'styled-components'

export const HeadingTertiaryContainer = styled.h3`
  font-family: ${({ theme }) => theme.fontFamily};
  font-weight: 600;
  font-size: 1.4rem;
  text-transform: uppercase;
`
