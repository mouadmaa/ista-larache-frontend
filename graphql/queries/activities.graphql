query activities(
  $take: Int
  $skip: Int
  $orderBy: ActivityOrderByInput
) {
  activities(
    take: $take
    skip: $skip
    orderBy: $orderBy
  ) {
    ...Activity
  }
  _activitiesMeta {
    count
  }
}
